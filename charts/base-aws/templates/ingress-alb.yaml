{{- if  .Values.ingress_alb }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  labels:
    app: {{ $.Release.Name }}
    helm.sh/chart: {{ $.Chart.Version  }}
  {{- with .Values.ingress_alb.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{ end  }}
  name: {{ .Release.Name }}
spec:
  ingressClassName: alb
  rules:
  - host: {{ .Values.ingress_alb.url }}
    http:
      paths:
      - backend:
          service:
            name: {{ .Release.Name }}
            port:
              number: {{ .Values.deployment.port | default 3000 }}
        path: /
        pathType: Prefix

{{ end }}